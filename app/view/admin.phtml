<?php
if (!isset($_SESSION)) {
    session_start();
} ?>



    <div class="background2">
    <?php include("Layout-header.phtml"); ?>
    <h1> Administration des articles</h1>


    <div class="container">

    <div class="msg">
        <?php
        if (!empty($_SESSION['msg'])) {

           echo '<h3>' .$_SESSION['msg']. '</h3>';
            unset($_SESSION['msg']);
        }
        ?>
    </div>

        <table class="table table-bordered">
            <a href="save_article">
                <button type="button" class="btn btn-primary btn-lg btn-block btn3d"><span
                            class="glyphicon glyphicon-ok"></span>Créer un nouvelle article
                </button>
            </a><br/>
            <thead>
            <tr>
                <th class="text-center">Titre</th>
                <th class="text-center">Billet</th>
                <th class="text-center">Action</th>
            </tr>
            </thead>


            <?php foreach ($articles
            as $article): ?>

            <tr>
                <td class="background4">
                    <p class="text"><?php echo $article['title']; ?></p>
                </td>

                <td class="text2">
                    <p><?php echo html_entity_decode($article['content']); ?></p>
                    <p><?php echo $article['author']; ?></p>
                    <p>Posted by Auteur : <?php echo $article['date_article']; ?></p>
                </td>

                <td class="text-center text2">
                    <a href="modification?article=<?= $article['id']; ?>" class="btn btn-success btn-lg btn3d"><span
                                class="glyphicon glyphicon-ok"></span>Modifier</a>

                    <a href="delete_Billet?article=<?= $article['id']; ?>" name="id"
                       class="btn btn-danger btn-lg btn3d"><span class="glyphicon glyphicon-ok"></span>Suprimer</a>
                </td>
    </div>
    </tr>
    <?php endforeach; ?>
    </table>


    <table class="table table-bordered">

    <h1> Administration des Commentaires</h1>
    <br/>
    <thead>

    <tr>
        <th class="text-center">Pseudo</th>
        <th class="text-center">Commentaire</th>
        <th class="text-center">Action</th>
    </tr>
    </thead>


    <?php foreach ($comments as $comment): ?>


        <tr>
            <td class="background4">
                <p class="text"><?php echo $comment['pseudo'] ?></p>
            </td>

            <td class="text2">
                <p><?php echo html_entity_decode($comment['content']); ?></p>


                <p class="bouton3">Poster le : <?php echo $comment['comment_date'] ?></p>

            </td>

            <td class="text-center text2">
                <a href="delete_commentaire?comment=<?= $comment['id']; ?>" name="id"
                   class="btn btn-danger btn-lg btn3d"><span class="glyphicon glyphicon-ok"></span>Suprimer</a>
                   <?php if ($comment['flag'] == 1){
                    echo '<img src="/img/drapeau.png" class="drapeau" alt="icon_drapeau" />';
                } ?>
            </td>
            </div>
        </tr>
    <?php endforeach; ?>

    </table>

</div>



<?php include("Layout-footer.phtml"); ?>

